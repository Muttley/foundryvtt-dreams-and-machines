<div>
	<span class="label">
		{{label}}
	</span>
	<input
		class="lockable"
		data-type="Number"
		name="{{name}}"
		placeholder="&mdash;"
		type="number"
		value="{{value}}"
		{{#unless alwaysUnlocked}}
			{{disabled @root.editModeDisabled}}
		{{/unless}}
	/>
</div>
