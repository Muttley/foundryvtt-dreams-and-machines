<div>
	<span class="label">
		{{#if @root.editModeEnabled}}
			<a
				data-action="addItem"
				data-item-type="npc_action"
				{{disabled @root.editModeDisabled}}
			>
				<i class="fa-solid fa-plus"></i>
			</a>
			&nbsp;
		{{/if}}
		{{localize "DNM.Labels.Actions"}}
	</span>

	<ol
		class="item-list"
		{{#unless @root.editModeDisabled}}
			data-tooltip="{{tooltip "edit-delete-mouse-controls"}}"
		{{/unless}}
	>
		{{#if (lookup inventory "npc_action")}}
			{{#each (lookup inventory "npc_action")}}
				<li
					class="item draggable"
					{{#if @root.editModeEnabled}}
						data-action="editItem"
					{{else}}
						data-action="useAction"
					{{/if}}
					data-uuid="{{this.uuid}}"
					data-item-id="{{this.id}}"
				>
					<a>
						<b class="item-name">{{this.name}}</b>:
						<em>
							{{{this.enrichedDescription}}}
						</em>
					</a>
				</li>
			{{/each}}
		{{else}}
			<div class="placeholder">
				{{localize "DNM.Placeholders.None"}}
			</div>
		{{/if}}
	</ol>
</div>
