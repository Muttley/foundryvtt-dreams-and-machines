<div>
	<span class="label">
		{{localize "DNM.Labels.Weapons"}}
	</span>

	<ol class="item-list">
		{{#each weapons}}
			<li
				class="item"
				data-menu="item"
				data-uuid="{{this.uuid}}"
			>
				<a
					data-action="onRoll"
					data-type="main"
					data-uuid="{{this.uuid}}"
				>
					<i class="fas fa-dice-d20"></i>
					<b style="text-transform: uppercase">{{this.name}} ({{this.system.weapon.type}})</b>:
					<i>
						{{#if this.system.weapon.damageQualities}}
							{{#each this.system.weapon.damageQualities}}
								<em>{{label}}{{#if (gt rating 0)}} {{ rating }}{{/if}}{{#unless @last}},{{/unless}}</em>
							{{/each}}
						{{else}}
							&mdash;
						{{/if}}
					</i>
				</a>
			</li>
		{{/each}}
	</ol>
</div>
