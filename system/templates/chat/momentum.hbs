<div class="w-full text-center text-xl {{#if isGM}}text-teal-800{{else}}text-fuchsia-900{{/if}}">
	{{#if store}}
		<span>{{ localize 'DNM.ChatMessages.StoreMomentum' }}</span>
	{{else}}
		<span>{{ localize 'DNM.ChatMessages.SpendMomentum' }}</span>
	{{/if}}
</div>
