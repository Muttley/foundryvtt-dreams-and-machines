<div class="grid-4-columns">

	<span class="label grid-colspan-4">
		{{localize "DNM.Labels.AttributeChoices"}}
	</span>

	<div class="grid-1-columns grid-colspan-2">

		<div class="grid-2-columns">
			{{#each attributes}}
				<a
					{{#if this.selected}}
						class="selectable-choice selected"
					{{else}}
						class="selectable-choice"
					{{/if}}
					data-action="toggleAttributeChoice"
					data-attribute-id="{{this.id}}"
					{{disabled @root.editModeDisabled}}
				>
					{{this.name}}
				</a>
			{{/each}}
		</div>

	</div>

	<label class="sublabel">
		<span>
			{{localize "DNM.Labels.Choices"}}
		</span>
		<input
			data-type="Number"
			name="system.attributeChoices.choiceCount"
			type="number"
			value="{{system.attributeChoices.choiceCount}}"
			{{disabled @root.editModeDisabled}}
		/>
	</label>

	<label class="sublabel">
		<span>
			{{localize "DNM.Labels.Value"}}
		</span>
		<input
			data-type="Number"
			name="system.attributeChoices.value"
			type="number"
			value="{{system.attributeChoices.value}}"
			{{disabled @root.editModeDisabled}}
		/>
	</label>

</div>
