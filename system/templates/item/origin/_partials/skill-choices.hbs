<div class="grid-4-columns">

	<span class="label grid-colspan-4">
		{{localize "DNM.Labels.SkillChoices"}}
	</span>

	<div class="grid-1-columns grid-colspan-2">

		<div class="grid-2-columns">
			{{#each skills}}
				<a
					{{#if this.selected}}
						class="selectable-choice selected"
					{{else}}
						class="selectable-choice"
					{{/if}}
					data-action="toggleSkillChoice"
					data-skill-id="{{this.id}}"
					{{disabled @root.editModeDisabled}}
				>
					{{this.name}}
				</a>
			{{/each}}
		</div>

	</div>

	<label class="sublabel">
		<span>
			{{localize "DNM.Labels.Choices"}}
		</span>
		<input
			data-type="Number"
			name="system.skillChoices.choiceCount"
			type="number"
			value="{{system.skillChoices.choiceCount}}"
			{{disabled @root.editModeDisabled}}
		/>
	</label>

	<label class="sublabel">
		<span>
			{{localize "DNM.Labels.Value"}}
		</span>
		<input
			data-type="Number"
			name="system.skillChoices.value"
			type="number"
			value="{{system.skillChoices.value}}"
			{{disabled @root.editModeDisabled}}
		/>
	</label>

</div>
