{{!-- Item Header --}}
<header class="sheet-header">
	<img
		alt="{{item.name}}"
		class="item-image"
		data-edit="img"
		src="{{item.img}}"
	/>
	<input
		class="name-input"
		name="name"
		type="text"
		value="{{item.name}}"
	/>
</header>
