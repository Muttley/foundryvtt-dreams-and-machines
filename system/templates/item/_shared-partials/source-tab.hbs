<div class="tab source" data-group="primary" data-tab="source">
	<div class="tab-body">
		<select name="system.source">
			{{selectOptions
				allSources
				labelAttr="name"
				localize=false
				selected=item.system.source
				valueAttr="uuid"
			}}
		</select>
	</div>
</div>
