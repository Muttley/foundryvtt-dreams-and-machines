<div class="grid-1-columns narrow-text-field {{#if editModeEnabled}}editing{{/if}}">
	<span class="label">
		{{localize "DNM.Labels.Equipment"}}
	</span>

	<div class="scrollable" style="width: 100%;">
		{{#if editModeEnabled}}
			<prose-mirror
				name="system.startingGear"
				value="{{ systemSource.startingGear }}"
				document-uuid="{{ document.uuid }}"
			></prose-mirror>
		{{else}}
			<div class="editor">
				<div class="editor-content" style="overflow: visible">
					{{{enrichedStartingGear}}}
				</div>
			</div>
		{{/if}}
	</div>
</div>
